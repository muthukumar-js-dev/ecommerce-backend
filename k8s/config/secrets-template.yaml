# IMPORTANT: This is a template file. DO NOT commit actual secrets to Git.
# Use kubectl to create secrets manually or use a secret management solution.

# Example commands to create secrets:

# JWT Secret
# kubectl create secret generic jwt-secret \
#   --from-literal=JWT_SECRET=your-super-secret-key-here \
#   --from-literal=JWT_EXPIRES_IN=7d \
#   --namespace=ecommerce-prod

# Stripe Secret
# kubectl create secret generic stripe-secret \
#   --from-literal=STRIPE_SECRET_KEY=sk_live_your_stripe_secret_key \
#   --from-literal=STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret \
#   --namespace=ecommerce-prod

# AWS Credentials
# kubectl create secret generic aws-credentials \
#   --from-literal=AWS_ACCESS_KEY_ID=AKIA_your_access_key \
#   --from-literal=AWS_SECRET_ACCESS_KEY=your_secret_access_key \
#   --from-literal=AWS_REGION=ap-south-1 \
#   --namespace=ecommerce-prod

# Database Credentials
# kubectl create secret generic db-credentials \
#   --from-literal=MONGODB_USERNAME=admin \
#   --from-literal=MONGODB_PASSWORD=your_secure_password \
#   --from-literal=REDIS_PASSWORD=your_redis_password \
#   --namespace=ecommerce-prod

# Email Service (SendGrid/SES)
# kubectl create secret generic email-credentials \
#   --from-literal=SENDGRID_API_KEY=SG.your_sendgrid_api_key \
#   --namespace=ecommerce-prod

# Template for reference (DO NOT USE IN PRODUCTION):
---
apiVersion: v1
kind: Secret
metadata:
  name: jwt-secret
  namespace: ecommerce-prod
type: Opaque
stringData:
  JWT_SECRET: "REPLACE_WITH_ACTUAL_SECRET"
  JWT_EXPIRES_IN: "7d"
---
apiVersion: v1
kind: Secret
metadata:
  name: stripe-secret
  namespace: ecommerce-prod
type: Opaque
stringData:
  STRIPE_SECRET_KEY: "REPLACE_WITH_ACTUAL_KEY"
  STRIPE_WEBHOOK_SECRET: "REPLACE_WITH_ACTUAL_SECRET"
---
apiVersion: v1
kind: Secret
metadata:
  name: aws-credentials
  namespace: ecommerce-prod
type: Opaque
stringData:
  AWS_ACCESS_KEY_ID: "REPLACE_WITH_ACTUAL_KEY"
  AWS_SECRET_ACCESS_KEY: "REPLACE_WITH_ACTUAL_SECRET"
  AWS_REGION: "ap-south-1"
---
apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
  namespace: ecommerce-prod
type: Opaque
stringData:
  MONGODB_USERNAME: "REPLACE_WITH_ACTUAL_USERNAME"
  MONGODB_PASSWORD: "REPLACE_WITH_ACTUAL_PASSWORD"
  REDIS_PASSWORD: "REPLACE_WITH_ACTUAL_PASSWORD"
---
apiVersion: v1
kind: Secret
metadata:
  name: email-credentials
  namespace: ecommerce-prod
type: Opaque
stringData:
  SENDGRID_API_KEY: "REPLACE_WITH_ACTUAL_KEY"
